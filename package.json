{
  "name": "hicode",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "panel": "cd panel && bun run dev",
    "panel:build": "cd panel && bun run build",
    "runner": "tsx runner.ts",
    "typecheck": "tsc --noEmit",
    "typecheck:panel": "cd panel && bun run typecheck",
    "test": "npm run typecheck && npm run typecheck:panel"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "playwright": "^1.61.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
